/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config) => {
    //    return config.externals.push("pino-pretty", "lokijs", "encoding");
    // }
};

export default nextConfig;
